# Tenor Saxophone Major Scales — Fingering Chart (HTML)

แพลนฉบับสรุปจากการ grill รอบ Q1–Q28
อ้างอิงหน้าตา/ฟีเจอร์จาก `https://tomplay.com/tools/scales/saxophone/major/c?notation=am`

**สถานะ: รอ approve → ยังไม่เขียนโค้ด**

---

## 1. เป้าหมาย

หน้า HTML ไฟล์เดียว เปิด offline ได้ ใช้ดู **major scale ทั้ง 12 key ของ tenor saxophone**
แต่ละสเกลแสดง **โน้ตบนบรรทัด 5 เส้น** คู่กับ **แผนผังการจับนิ้ว (fingering)** ของทุกตัวโน้ต
พิมพ์ลงกระดาษ A4 เป็นชีทซ้อมได้

ไม่ใช่เป้าหมายใน v1: เสียง, metronome, โหมดทดสอบ, minor / pentatonic / blues / chromatic

---

## 2. ข้อตกลงหลัก (Decisions)

| # | หัวข้อ | ข้อสรุป |
|---|--------|---------|
| Q1 | ระดับเสียง | ใช้ **written pitch** (โน้ตที่เขียน = ที่นิ้วจับ) แสดง concert pitch เป็นข้อความกำกับ เช่น `C Major (written) = Bb Major (concert)` |
| Q2 | จำนวน key | **12 key** ตาม circle of fifths/fourths ไม่แยก enharmonic เป็น 15 |
| Q3 | ช่วงเสียงต่อสเกล | ค่าเริ่มต้น **1 octave** (tonic อยู่ในช่วง C4–B4) + สลับเป็น 2 octave / full range ได้ |
| Q4 | รูปนิ้ว | **SVG วาดเอง** ไม่ใช้ภาพจากที่อื่น (ลิขสิทธิ์ + ต้องการ scale ได้ทุกขนาด) |
| Q5 | ปลายทาง | **ไฟล์ HTML เดียว self-contained** ไม่มี CDN ตอน runtime → เปิด offline ได้ และ publish เป็น Artifact ได้ |
| Q6 | ภาษา | **ไทยเป็นหลัก** ศัพท์ดนตรีคงภาษาอังกฤษ (Major, octave, key, palm key ฯลฯ) ไม่ทำระบบ i18n |
| Q7 | ชื่อโน้ต | **A-B-C เป็นค่าเริ่มต้น** + ปุ่มสลับเป็น **Do-Re-Mi** |
| Q8 | ขอบเขต v1 | ดูสเกล + fingering + **print CSS** เท่านั้น (ตัด audio / quiz ออก) |
| Q9 | บรรทัด 5 เส้น | ใช้ **ไลบรารีวาดโน้ต ฝังลงไฟล์** ไม่วาด staff เอง |
| Q10 | Layout | **แสดงทีละ key** มี selector เลือก ไม่ใช่หน้าเดียวยาว 12 section |
| Q11 | key ที่ทำ 2 octave ไม่ได้ | ใช้โหมด **full range** แทนการปิดปุ่ม |
| Q12 | alternate fingering | แสดง **นิ้วหลัก + ปุ่มกางดูนิ้วทางเลือก** (ไม่เลือกอัตโนมัติตามบริบท) |
| Q13 | ลำดับ key | เรียงตาม **circle of fourths**: C, F, Bb, Eb, Ab, Db, Gb, B, E, A, D, G |
| Q14 | การพิมพ์ | **A4 แนวตั้ง 1 key ต่อ 1 หน้า** ซ่อน nav/ปุ่ม toggle ตอนพิมพ์ |
| Q15 | โครงสร้างข้อมูล | ตาราง `FINGERINGS` map เดียว (โน้ต → ปุ่มที่กด) + **generate สเกลจากสูตร** ตอน runtime |
| Q16 | ไลบรารี | **abcjs 6.4.4** (`abcjs-basic-min.js` ~472 KB) ฝังเป็น `<script>` ก้อนเดียว |
| Q17 | จัดรูปนิ้วให้ตรงโน้ต | **แยก 2 แถว + CSS grid คอลัมน์เท่ากัน** ไม่อ่านตำแหน่ง x จาก internal DOM ของ abcjs |
| Q18 | print scope | `Ctrl+P` = พิมพ์เฉพาะ key ที่เปิดอยู่ (1 หน้า) + **ปุ่ม "พิมพ์ทั้งหมด"** สำหรับ 12 หน้า |
| Q19 | full range + high F# | กฎ: ไล่จากโน้ตต่ำสุดของสเกลที่ ≥ Bb3 ถึงสูงสุดที่ ≤ F6 · เลือกได้ทั้ง 12 key · **ตัดที่ F6 เป็นค่าเริ่มต้น** + checkbox "แซ็กของฉันมีคีย์ high F#" |
| Q20 | การสะกด key | **Gb, Db, B** (ไม่ใช่ F#, C#, Cb) หัวข้อเขียน `Gb / F# Major` กำกับ |
| Q21 | ความถูกต้องข้อมูลนิ้ว | เทียบกับ fingering chart มาตรฐานก่อนกรอก + ทำ **หน้า debug** ไล่โน้ตต่ำ→สูงให้ตรวจรอบสุดท้าย |
| Q22 | สไตล์ SVG | **schematic** (วงกลม/แคปซูล ทึบ=กด โปร่ง=ไม่กด) ไม่วาดรูปตัวแซ็กจริง |
| Q23 | จำนวนปุ่ม | **ครบ 23 จุด** รวม `Side F#` แยกจาก `High F#`; ปุ่มที่ไม่ได้ใช้วาดเส้นบางสีจาง (ไม่ซ่อน) |
| Q24 | ป้ายชื่อปุ่ม | รูปเล็กไม่ใส่ตัวอักษร + **hover ขึ้น tooltip** + มี **"แผนที่ปุ่ม" ขนาดใหญ่พร้อมชื่อครบ** 1 อันบนสุดของหน้า |
| Q25 | ขอบเขต alternate | เฉพาะที่ใช้จริงในสเกล: **Bb (bis / side / 1+1), F#, C6, Eb** |
| Q26 | ธีม | **light + dark + ปุ่มสลับเอง** ใช้ CSS variable · system font เท่านั้น (ไม่ฝัง webfont) · ตอนพิมพ์บังคับ light |
| Q27 | มือถือ | **แถวโน้ต + แถวรูปนิ้ว เลื่อนแนวนอนพร้อมกัน** (ไม่ตัดบรรทัด ไม่สลับเป็นแนวตั้ง) |
| Q28 | ที่เก็บ + เกณฑ์เสร็จ | `P:\SaxScales\` — ดูหัวข้อ 7 |

---

## 3. ข้อเท็จจริงที่ตรวจแล้ว (ใช้ตัดสินใจ)

- **ช่วงเสียง tenor sax (written)** = `Bb3` – `F6` · มี `F#6` ถ้าแซ็กมีคีย์ high F#
  → รวม **32 ตัวโน้ตไม่ซ้ำ** (33 ถ้ารวม F#6) นี่คือขนาดจริงของงานกรอกข้อมูล ไม่ใช่ 12 × จำนวนโน้ต
- **1 octave**: กฎ tonic ∈ [C4, B4] ทำให้ครบทั้ง 12 key ไม่มีตัวไหนหลุดช่วง
- **2 octave**: ทำได้ **9 key** — `G, Ab, A` ทำไม่ได้ (ต้องเริ่ม G3/Ab3/A3 ซึ่งต่ำกว่า Bb3 หรือขึ้นไป G6/A6 ซึ่งเป็น altissimo)
  ถ้าติ๊ก high F# จะได้ **10 key** (Gb major จบพอดีที่ Gb6 = F#6)
- **abcjs** `6.4.4` `abcjs-basic-min.js` ≈ **472 KB** ดึงผ่าน jsDelivr ได้ (ตรวจแล้ว HTTP 200) — Artifact จำกัด 16 MB จึงเหลือเฟือ
- เครื่องนี้ **ไม่มี node/npm** → ไม่มี build step ใช้ `curl` ดึงไฟล์ min มาแปะตรง ๆ

---

## 4. สถาปัตยกรรมข้อมูล

### 4.1 รายการปุ่ม (23 จุด)

```
มือซ้าย : oct (โป้ง) · frontF · palmD · palmEb · palmF
          B · A · G · bis
          G# · lowC# · lowB · lowBb            (pinky table)
มือขวา  : F · E · D
          sideE · sideC · sideBb · sideF#       (ข้างนิ้วชี้/นิ้วนาง)
          highF# · altF#                       (คีย์ high F# แบบเลือกใช้; altF# เป็น alias)
          Eb · lowC                             (pinky)
```

### 4.2 ตารางนิ้ว — แหล่งความจริงเดียว

```js
const FINGERINGS = {
  'Bb3': { keys: ['B','A','G','F','E','D','Eb','lowC','lowBb'] },
  'C4' : { keys: ['B','A','G','F','E','D','Eb','lowC'] },
  ...
  'Bb4': { keys: ['oct','B','bis'],
           alts: [
             { label: 'side Bb', keys: ['oct','B','sideBb'], note: 'ใช้เมื่อโน้ตข้างเคียงมี B natural' },
             { label: '1+1',     keys: ['oct','B','F'],      note: 'ใช้ตอนวิ่งเร็วขึ้น-ลง' }
           ] },
  ...
};   // ทั้งหมด 32 (หรือ 33) รายการ
```

### 4.3 สร้างสเกลตอน runtime

- `MAJOR_STEPS = [0,2,4,5,7,9,11]` (W-W-H-W-W-W-H)
- ชื่อโน้ตสะกดตามลำดับตัวอักษร A-G ไม่ซ้ำ (ทำให้ Gb major ได้ `Gb Ab Bb Cb Db Eb F` ถูกต้อง)
- โหมด: `1oct` → tonic ∈ [C4,B4] ขึ้น 8 ตัว · `2oct` → 15 ตัว · `full` → ตามกฎ Q19
- แปลงเป็น **ABC string** ป้อน abcjs เช่น
  `X:1\nL:1/4\nK:Gb\nG A B c d e f g|]`
  โดย C4–B4 = ตัวพิมพ์ใหญ่, C5–B5 = ตัวพิมพ์เล็ก, C6+ = เติม `'`, ต่ำกว่า C4 = เติม `,`
  key signature มาจาก `K:` ทำให้ไม่ต้องเขียน accidental ทีละตัว

### 4.4 การจัดวาง (Q17)

```
┌─────────────────────────────────────────┐
│  [ abcjs SVG staff — 1 บรรทัด ]          │  ← บังคับระยะโน้ตเท่ากัน
├─────┬─────┬─────┬─────┬─────┬─────┬─────┤
│ SVG │ SVG │ SVG │ SVG │ SVG │ SVG │ SVG │  ← CSS grid: repeat(N, 1fr)
│ Gb  │ Ab  │ Bb  │ Cb  │ Db  │ Eb  │ F   │
└─────┴─────┴─────┴─────┴─────┴─────┴─────┘
        ทั้งบล็อกเลื่อนแนวนอนพร้อมกันบนจอแคบ
```

---

## 5. โครงไฟล์

```
P:\SaxScales\
├─ PLAN.md                  ← เอกสารนี้
├─ saxophone-scales.html    ← ไฟล์เดียวจบ (~500 KB รวม abcjs)
└─ vendor\
   └─ abcjs-basic-min.js    ← ไฟล์ดิบที่ curl มา (เก็บไว้อ้างอิง ไม่ถูกใช้ตอน runtime)
```

ภายใน `saxophone-scales.html` เรียงเป็น: `<style>` → markup → `<script>` abcjs (ฝัง) → `<script>` โค้ดเรา

---

## 6. ลำดับการทำ

| Phase | งาน | ตรวจได้ด้วย |
|-------|-----|------------|
| P0 | `curl` abcjs มาเก็บใน `vendor\` | ไฟล์มีจริง ขนาด ~472 KB |
| P1 | โครง HTML + CSS variable + theme toggle + key selector 12 ปุ่ม | เปิดแล้วสลับ light/dark ได้ กดเลือก key ได้ |
| P2 | กรอก `FINGERINGS` 32 โน้ต (เทียบ chart มาตรฐานก่อน) + **หน้า debug** เรียงต่ำ→สูง | ไล่ตรวจด้วยตา ครบ 32 ไม่มีช่องว่าง |
| P3 | SVG fingering component (23 จุด) + tooltip + แผนที่ปุ่มใหญ่ | รูปนิ้วขึ้นถูกในหน้า debug |
| P4 | generate สเกล + ABC string + abcjs render + grid จัดตำแหน่ง | ทั้ง 12 key แสดงถูก key signature ถูก |
| P5 | โหมด 1oct / 2oct / full range + checkbox high F# + สลับ Do-Re-Mi | G/Ab/A แสดง full range ถูก · Gb ปลดล็อก 2 oct เมื่อติ๊ก |
| P6 | print CSS + ปุ่มพิมพ์ทั้งหมด | Print preview: 1 key/หน้า A4 · ทั้งหมด = 12 หน้า |
| P7 | ตรวจรับตามหัวข้อ 7 | — |

---

## 7. เกณฑ์ว่า "เสร็จ" (Q28)

1. เปิดในเบราว์เซอร์แบบ offline (double-click ไฟล์) แล้วทำงานได้ **ไม่มี error ใน console**
2. ครบ **12 key** × **3 โหมด** (1oct / 2oct / full range) แสดงผลถูกต้องทุกช่อง
3. **G, Ab, A** — 2 octave ถูกปิดพร้อมข้อความอธิบาย และ full range ให้ B3→E6 / Bb3→F6 / B3→E6
4. ติ๊ก **high F#** แล้ว **Gb major** ทำ 2 octave ได้
5. **สั่งพิมพ์** ได้ 1 key ต่อหน้า A4 แนวตั้ง · ปุ่มพิมพ์ทั้งหมดได้ 12 หน้า · รูปนิ้วอ่านออกเมื่อพิมพ์ขาวดำ
6. **ตรวจข้อมูลนิ้ว 32 โน้ตในหน้า debug** ผ่านสายตาผู้ใช้
7. บนมือถือ แถวโน้ตกับแถวรูปนิ้ว **เลื่อนแนวนอนตรงกัน** ไม่หลุดจากกัน

---

## 8. ความเสี่ยงที่รู้ตัว

| ความเสี่ยง | ผลกระทบ | วิธีรับมือ |
|-----------|---------|-----------|
| **ข้อมูลนิ้วผิด** | ทั้งเว็บใช้ไม่ได้ · test อัตโนมัติจับไม่ได้ | เทียบ chart มาตรฐานก่อนกรอก + หน้า debug ให้คนตรวจ (Q21) |
| abcjs ไม่ยอมให้ระยะโน้ตเท่ากันเป๊ะ | รูปนิ้วเหลื่อมจากโน้ต | สเกลมีโน้ตความยาวเท่ากันหมด โอกาสเหลื่อมต่ำ · ถ้าเหลื่อมจริงค่อยถอยไปวางรูปนิ้วเป็นตารางแยก (Q17 ตัวเลือก c) |
| ไฟล์ 500 KB | โหลดช้าบนเน็ตช้า | เปิด offline เป็นหลัก ไม่ใช่ปัญหา · ถ้า publish Artifact ยังต่ำกว่าลิมิต 16 MB มาก |
| ระยะเสียง altissimo / คีย์ high F# ต่างรุ่น | ผู้ใช้บางคนเป่าไม่ได้ตามที่แสดง | ตัดที่ F6 เป็นค่าเริ่มต้น ให้ผู้ใช้ opt-in เอง (Q19) |
| ชื่อโน้ตแบบ Do-Re-Mi กับ key ที่มีแฟลต | `Cb` ในระบบ Do-Re-Mi สะกดยังไง | ใช้ `Do♭ Re♭ ...` ตรงตัวตามชื่อโน้ตอังกฤษ ไม่แปลงระบบใหม่ |

---

## 9. เก็บไว้เป็น v2 (ตกลงกันแล้วว่าไม่ทำตอนนี้)

- minor / harmonic minor / melodic minor / pentatonic / blues / chromatic — โครงข้อมูลรองรับแล้ว เพิ่มแค่สูตร
- เสียงเล่นสเกล + metronome
- โหมดทดสอบ (สุ่มโน้ต ทายนิ้ว)
- เลือกนิ้ว Bb อัตโนมัติตามบริบทของสเกล (Q12 ตัวเลือก c)
- รองรับ alto / soprano (เปลี่ยนแค่ transposition + ช่วงเสียง ตาราง fingering ใช้ร่วมกันได้)
